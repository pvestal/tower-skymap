[Unit]
Description=Tower Skymap drain worker (download + thumbnail)
After=network.target postgresql.service

[Service]
Type=oneshot
User=patrick
Group=patrick
WorkingDirectory=/opt/skymap
EnvironmentFile=/opt/skymap/.env
ExecStart=/opt/skymap/venv/bin/python -m workers.drain 50
